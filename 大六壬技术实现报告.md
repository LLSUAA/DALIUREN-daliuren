# 大六壬赛博终端 —— 技术实现报告

> 面向管理层：详解程序如何将千年术数"大六壬"转化为可运行的软件系统，并诚实披露当前实现与完整理论的差距。

---

## 一、大六壬是什么（一句话版）

大六壬是一套**时空运算模型**。你把"什么时间 + 在哪儿 + 问什么事"输入进去，它通过一套以天干地支为符号的规则系统，推演出事情的发展走向。古人称它为"占卜之王"，与奇门遁甲、太乙神数并称"上古三式"。

---

## 二、完整大六壬的五步推演流程

```
输入时间 → ①排天地盘 → ②起四课 → ③发三传 → ④排神将六亲遁干神煞 → ⑤综合解读
```

| 步骤 | 做什么 | 通俗理解 |
|------|--------|----------|
| ① 排天地盘 | 用"月将+占时"定位时空背景 | 确定"此刻宇宙长什么样" |
| ② 起四课 | 用日干支在天地盘上"爬梯子" | 建立"我"与"事"的关系模型 |
| ③ 发三传 | 用九宗门法则找初传→中传→末传 | 推演"开始→发展→结局" |
| ④ 排神将六亲 | 配十二天将、算六亲关系、加遁干神煞 | 给骨架填上血肉细节 |
| ⑤ 综合解读 | 结合所有信息给出判断 | 输出最终结论和建议 |

---

## 三、我们的程序是怎么实现的

### 整体架构

```
用户浏览器/桌面端 (Vue + Tauri)
        │  HTTP请求
        ▼
  FastAPI后端 (api/server.py)
        │
        ├──→ 时空解析器 (engine/utils/time_parser.py)
        │      └─ 公历→农历→四柱八字→占时/月将/本命/行年
        │
        ├──→ 大六壬引擎 (main.py)
        │      ├─ 天地盘系统 (engine/systems/astrolabe_sys.py)
        │      ├─ 四课系统   (engine/systems/four_lessons_sys.py)
        │      └─ 路由系统   (engine/systems/routing_sys.py)
        │
        └──→ AI神谕模块 (engine/utils/oracle.py)
               └─ 意图路由 → 构建Prompt → 调用大模型 → 返回判词
```

### 各模块逐一对照

#### 步骤① 排天地盘 —— ✅ 完整实现

**对应文件：** [astrolabe_sys.py](file:///e:/DALIUREN/engine/systems/astrolabe_sys.py)

**实现原理：**
- 核心公式：`offset = (月将索引 - 占时索引) % 12`
- 把天盘的"月将"对准地盘的"占时"位置，然后顺时针排满12宫
- 输出12组 (地盘, 天盘) 对应关系

**验证：** 假设冬至后月将是"丑"(索引1)，占时是"午"(索引6)，则 offset = (1-6)%12 = 7。地盘"午"(6)的头顶是天盘(6+7)%12=1即"丑"。这与理论"月将丑加占时午"完全一致。✅

---

#### 步骤② 起四课 —— ✅ 完整实现

**对应文件：** [four_lessons_sys.py](file:///e:/DALIUREN/engine/systems/four_lessons_sys.py)

**实现原理：**
- 天干寄宫：用 `STEM_PARASITIC_MAPPING` 把日干映射到地盘（如甲→寅）
- 第一课：日干寄宫的地盘位置 → 头顶天盘 = 干上神
- 第二课：干上神作为新地盘位置 → 头顶天盘 = 干阴神
- 第三课：日支的地盘位置 → 头顶天盘 = 支上神
- 第四课：支上神作为新地盘位置 → 头顶天盘 = 支阴神

**验证：** 与理论"连爬两次梯"完全一致。✅

---

#### 步骤③ 发三传 —— ⚠️ 部分实现（存在重大缺口）

**对应文件：** [routing_sys.py](file:///e:/DALIUREN/engine/systems/routing_sys.py)

**当前实现了什么：**
- ✅ 五行生克扫描（`scan_clashes`）：判断四课中哪些课有"下克上(贼)"或"上克下(克)"
- ✅ 九宗门路由决策（`dispatch_route`）：五层降级策略决定用哪种方法
  - 第一层：贼克法（1个下克上 或 1个上克下）
  - 第二层：比用法（多个克，比极性筛选）
  - 第三层：涉害法（仍有冲突，取课序最小者——**简化版**）
  - 第四层：遥克法（无克时，找日干与天盘相克）
  - 第五层：昴星法（终极兜底，取第1课——**简化版**）

**当前没实现什么（关键缺口）：**

> 🔴 **三传本身没有生成！** 路由系统只决定了"用第几课发用"，但从未实际计算出初传、中传、末传分别是什么地支。

理论上的三传生成逻辑应该是：
```
初传 = 发用课的上神（天盘）
中传 = 把初传地支放回地盘，其头顶天盘
末传 = 把中传地支放回地盘，其头顶天盘
```

但我们的代码在 `main.py` 的 `generate_snapshot()` 中，拿到 `route_decision`（只包含 method + init_node_lesson_id + is_bottom_up）后就直接组装快照返回了，**从未调用任何三传生成逻辑**。

---

#### 步骤④ 排神将六亲遁干神煞 —— ❌ 基本未实现

这是当前程序**最大的功能缺口**。完整大六壬在这一步需要做四件事：

| 子步骤 | 说明 | 当前状态 |
|--------|------|----------|
| 十二天将 | 贵人/螣蛇/朱雀/六合/勾陈/青龙/天空/白虎/太常/玄武/太阴/天后，按昼夜顺逆排布到天盘12宫 | ❌ 未实现 |
| 六亲 | 判断三传与日干的生克关系（父母/兄弟/妻财/官鬼/子孙） | ❌ 未实现 |
| 遁干 | 旬遁，给地支配上对应的天干 | ❌ 未实现 |
| 神煞 | 数十种吉神凶煞（禄神/驿马/桃花/羊刃/灾煞等） | ⚠️ 仅实现了空亡、绝处逢生、冲破三个基础判断 |

**当前仅有的硬逻辑计算（在 [main.py](file:///e:/DALIUREN/main.py) 中）：**
- `_calculate_kong_wang()`：旬空（空亡）计算 ✅
- `_check_jue_chu_feng_sheng()`：绝处逢生判断（简化版）⚠️
- `_check_chong_po()`：冲破判断（简化版）⚠️

---

#### 步骤⑤ AI解读 —— ⚠️ 部分实现

**对应文件：** [oracle.py](file:///e:/DALIUREN/engine/utils/oracle.py)

**实现了什么：**
- ✅ 意图路由网关：根据用户输入的关键词自动分类（天气/财运/通用）
- ✅ 分类专属Prompt：不同占域有不同的解读规则
- ✅ 四重反转校验机制：空亡反转、绝处逢生、贪合忘克、冲破凶局
- ✅ 四段式输出模板：定盘确真 → 核心推演链条 → 铁断与应期 → 风控与应对
- ✅ SSE流式输出：逐字推送，不用等几十秒
- ✅ 优雅降级：API挂了有备用文案

**存在的问题：**
- 🔴 AI拿到的数据不完整：传给大模型的只有路由方法名和课序号，**没有实际的三传地支、没有十二天将、没有六亲关系**。AI实际上是在"盲推"。
- 🟡 分类占目前只有天气和财运两个领域，健康/事业/感情/出行等常见占域尚未覆盖。

---

## 四、完整度总览

```
大六壬完整流程        实现状态      完成度
─────────────────────────────────────────
① 排天地盘           ✅ 完整        100%
② 起四课             ✅ 完整        100%
③ 发三传             ⚠️ 仅路由决策  30%
   ├─ 初传计算        ❌ 未实现       0%
   ├─ 中传计算        ❌ 未实现       0%
   └─ 末传计算        ❌ 未实现       0%
④ 排神将六亲遁干神煞  ❌ 基本未实现   5%
   ├─ 十二天将        ❌ 未实现       0%
   ├─ 六亲            ❌ 未实现       0%
   ├─ 遁干            ❌ 未实现       0%
   └─ 神煞            ⚠️ 仅3项       10%
⑤ AI解读             ⚠️ 数据不完整   40%
─────────────────────────────────────────
整体完成度                           ≈35%
```

---

## 五、漏洞清单与修复优先级

### 🔴 P0 - 致命缺口（影响核心功能）

| 编号 | 问题 | 影响 | 修复方案 |
|------|------|------|----------|
| P0-1 | **三传未生成** | 程序最核心的输出缺失，AI解读没有真实数据支撑 | 在 `main.py` 中新增 `_generate_three_transmissions()` 方法：从发用课取初传，然后两次"地盘回推天盘"得到中传和末传 |
| P0-2 | **涉害法简化** | 多克冲突时只取课序最小的，不是真正的涉害深度计算 | 实现涉害深度算法：统计每个冲突课中，天盘地支克地盘地支的"受害"次数，取最深者 |

### 🟡 P1 - 重要缺口（影响专业度）

| 编号 | 问题 | 影响 | 修复方案 |
|------|------|------|----------|
| P1-1 | **十二天将未排布** | 缺少贵人/青龙/白虎等关键神煞，解读维度单一 | 实现天将排布：根据昼夜确定贵人顺逆，按顺序排12将到天盘 |
| P1-2 | **六亲未计算** | 无法判断三传对日干的生克关系 | 实现六亲算法：根据日干五行与三传地支五行的生克关系判定 |
| P1-3 | **昴星法简化** | 无克时永远取第1课，不符合昴星法真义 | 实现标准昴星法：阳日取地盘酉上神，阴日取天盘酉下神 |
| P1-4 | **伏吟/反吟/八专/别责未实现** | 枚举定义了但无实际逻辑 | 逐一实现这四种特殊路由方法 |

### 🟢 P2 - 增强缺口（锦上添花）

| 编号 | 问题 | 影响 | 修复方案 |
|------|------|------|----------|
| P2-1 | **遁干未计算** | 缺少旬遁信息 | 实现旬遁算法 |
| P2-2 | **神煞不完整** | 只有3个基础判断 | 逐步添加禄神/驿马/桃花/羊刃/灾煞/劫煞等 |
| P2-3 | **分类占领域少** | 只支持天气和财运 | 扩展健康/事业/感情/出行等意图分类 |
| P2-4 | **AI收到的数据不完整** | 大模型在信息不全的情况下解读 | 将三传、天将、六亲等完整数据注入Prompt |

---

## 六、当前程序的真实能力边界

**能做什么：**
- ✅ 输入任意公历时间和全球经纬度，正确排出天地盘
- ✅ 正确生成四课结构
- ✅ 判断四课中的五行生克冲突
- ✅ 用五层降级策略决定路由方法（保证100%不崩溃）
- ✅ 计算空亡、绝处逢生、冲破等基础硬逻辑
- ✅ 调用AI生成自然语言解读

**不能做什么：**
- ❌ 输出真正的三传（初传/中传/末传）
- ❌ 排布十二天将
- ❌ 计算六亲关系
- ❌ 计算遁干
- ❌ 完整的神煞体系
- ❌ 基于完整数据的精准解读

**一句话总结：** 我们的程序目前完成了大六壬的"骨架搭建"（天地盘+四课+路由方向），但"血肉填充"（三传+天将+六亲+神煞）尚未完成。AI解读模块虽然设计精良，但因为输入数据不完整，实际上是在信息不充分的情况下工作。

---

## 七、建议的下一步路线图

```
第一阶段（1-2周）：补全三传
  └─ 实现初传/中传/末传生成 → 核心功能闭环

第二阶段（2-3周）：补全神将六亲
  └─ 十二天将排布 + 六亲计算 → 专业度大幅提升

第三阶段（1-2周）：完善路由
  └─ 涉害深度算法 + 昴星法标准实现 + 伏吟/反吟/八专/别责

第四阶段（持续）：丰富细节
  └─ 遁干 + 完整神煞 + 扩展分类占 + AI数据完整性
```

---

*报告日期：2026年4月23日*
*技术负责人：大六壬赛博终端开发团队*
