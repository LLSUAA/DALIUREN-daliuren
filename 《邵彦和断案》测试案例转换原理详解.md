## 🕰️ 第一步：时间坐标的“绝对锚定” (Time Parsing)
在《邵彦和断案》中，古人记录时间的方式是帝王年号加干支，比如：“绍兴元年 三月 甲子日 辰时”。计算机根本看不懂这是什么，我们需要把它还原为绝对的物理时间轴。

历史纪元转换：利用底层的历法库（如 lunar_python），我们将“绍兴元年”逆向推算为公历的 公元1131年。

节气与闰月校准：古人按农历算，但大六壬排盘必须看“月将”（即太阳所在黄道十二宫的位置，由节气决定）。系统会将农历三月与当时的“二十四节气”进行对齐，精确计算出太阳过宫的具体度数。

空间坐标代入（真太阳时）：邵彦和当年在南宋都城临安（今杭州）或汴梁（今开封）断案。我们会直接把当时案件发生地的现代经纬度（比如杭州的 120.15°E）输入系统。这样，系统就能将古代的“辰时”修正为该经纬度下绝对精确的真太阳时，剔除掉古代计时的粗糙误差。

## ⚙️ 第二步：天干地支的“数组映射与模运算” (Math Mapping)
这是整个物理引擎最核心的代码魔法。天干地支在计算机眼里不是文字，而是环形数组（Circular Arrays）和取模运算（Modulo Math）。

我们建立了两组核心数组：

天干数组 (Stems): ['甲', '乙', '丙', '丁', '戊', '己', '庚', '辛', '壬', '癸'] （索引 0-9）

地支数组 (Branches): ['子', '丑', '寅', '卯', '辰', '巳', '午', '未', '申', '酉', '戌', '亥'] （索引 0-11）

如何映射生克冲合？
大六壬里的“子午相冲”、“寅申相冲”，在代码里就是一道极其简单的数学题：

判断相冲公式：if abs(Branch_A_Index - Branch_B_Index) == 6:

比如：子(0) 和 午(6)，|0 - 6| = 6，判定为相冲！

计算天盘旋转（地支加法）：大六壬排盘需要将“月将”加在“占时”上转动。在代码里就是 (月将索引 - 占时索引 + 当前地盘索引) % 12。通过 % 12（对12取模），无论怎么加减，数值都会完美在一个圆环（0-11）里循环，这就构成了你前端看到的那个转动的全息星盘！

## 👤 第三步：人事参数的“结构化提取” (Entity Extraction)
古文案例中的人事背景通常是散文式的描述，比如：“有妇人遗失金钗，生于丙寅年，求测。”

我们需要将这些信息转化为后端的标准请求格式（JSON payload）：

性别映射 (gender)：“妇人”映射为 Female。这在大六壬中决定了排盘时某些神煞的顺逆排法（如“行年”的男顺女逆）。

出生年份 (birth_year)：“丙寅年”被系统转化为具体的公历年份（如公元1086年）。引擎通过这个年份，计算出她的本命（寅）和此时的行年。

占测事由 (event_intent)：将“遗失金钗”提取为核心意图。

## 🧠 终极对撞：古今信息的融合
当这三个步骤完成，宋朝的案子就彻底变成了一组现代参数：
```
JSON
{
  "time_str": "1131-04-15 08:30:00", 
  "longitude": 120.15,
  "gender": "女",
  "birth_year": 1086,
  "event_intent": "遗失金钗"
}
```
我们的 Python 物理引擎拿到这组数据，运用环形数组的模运算，瞬间排出四课三传，然后将结果（以及是否触发空亡、绝处逢生等布尔值）打包发给 LLM 大语言模型。

大模型通过你在 oracle.py 里设定的严苛提示词约束，看着这组精准的现代物理引擎数据，用大六壬的逻辑思考，最终吐出和邵彦和当年脑海中一模一样的推理链条！